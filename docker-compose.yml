services:
  backend:
    build:
      context: .
      dockerfile: apps/backend/Dockerfile
    env_file: .env
    restart: unless-stopped

  dashboard:
    build:
      context: .
      dockerfile: apps/frontend/Dockerfile
    ports: ['3000:80']
    depends_on: [backend]
    restart: unless-stopped
